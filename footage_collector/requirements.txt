yt-dlp>=2025.1.1
ddgs>=9.0.0
requests>=2.31.0
Pillow>=10.0.0
# System dependency (installed by setup scripts, NOT via pip):
#   ffmpeg + ffprobe   -> https://ffmpeg.org/download.html
#   (setup.bat / setup.sh place them in ./bin automatically)
